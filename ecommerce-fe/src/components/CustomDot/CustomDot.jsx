import React from "react";

const CustomDot = ({ images }) => {
  //   console.log(images);
  return (
    <div>
      {/* {images.map((image, index) => (
        <img key={index} src={image.src} alt={image.alt} className=" w-6 h-6" />
      ))} */}
    </div>
  );
};

export default CustomDot;
