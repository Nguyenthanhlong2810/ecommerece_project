import React from "react";
import ShoppingCartIcon from "@mui/icons-material/ShoppingCart";

const CartIcon = ({ color }) => {
  return <ShoppingCartIcon style={{ fontSize: 28 }} />;
};

export default CartIcon;
